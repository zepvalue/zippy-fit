# json_key is passed by the deploy lane (after a friendly existence check) rather than
# set here, so its validation doesn't break lanes that don't upload (e.g. `build`).
package_name("com.zepvalue.zippyfit")
